-- Performance indexes for fuzzy search
-- These will be created after tables exist (either from restore or fresh creation)

-- Note: This script might fail if tables don't exist yet
-- The Ruby application will ensure these indexes exist when it runs